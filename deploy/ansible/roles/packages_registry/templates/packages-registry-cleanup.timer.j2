[Unit]
Description=Daily package registry application release retention

[Timer]
OnCalendar=daily
RandomizedDelaySec=1h
Persistent=true
Unit=packages-registry-cleanup.service

[Install]
WantedBy=timers.target
