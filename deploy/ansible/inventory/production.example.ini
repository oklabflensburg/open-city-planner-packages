[packages_registry]
# Copy this file to production.ini and replace the example hostname.
packages-registry.example.invalid

[packages_registry:vars]
# Prefer ~/.ssh/config or ANSIBLE_REMOTE_USER over committing an operator name.
ansible_python_interpreter=/usr/bin/python3
